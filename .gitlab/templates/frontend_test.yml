frontend-test:
  stage: test
  image: node:20
  before_script:
    - cd frontend
    - npm install
  script:
    - npm test || echo "to do /frontend/src/ etc."
  only:
    - merge_requests
    - main
